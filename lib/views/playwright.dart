import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Playwright extends StatefulWidget {
  const Playwright({Key? key}) : super(key: key);

  @override
  _PlaywrightState createState() => _PlaywrightState();
}

class _PlaywrightState extends State<Playwright> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Playwright")),
      body: Column(
        children: const [],
      ),
    );
  }
}
